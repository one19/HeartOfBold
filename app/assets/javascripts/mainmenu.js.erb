
BasicGame.MainMenu = function (game) {

  this.music = null;
  this.playButton = null;

};

BasicGame.MainMenu.prototype = {

  // preload: function () {
  //   var biome = BasicGame.game.cache.getJSON('biome');
  //   eval(biome.functions);
  //   console.log(biome);
  // },

  create: function () {
    //  Already preloaded the assets, so let's kick right into the Main Menu.
    //  Here all thats happening is music and adding a picture and button
    this.add.sprite(0, 0, 'titlePage');

    this.loadingText = this.add.text(this.game.width / 2, this.game.height / 2 + 120, "Press Z or tap/click game to start", { font: "20px monospace", fill: "#fff" });
    this.loadingText.anchor.setTo(0.5, 0.5);
    //this.add.text(this.game.width / 2, this.game.height - 90, "image assets Copyright (c) 2015 Drew Showalter", { font: "12px monospace", fill: "#fff", align: "center"}).anchor.setTo(0.5, 0.5);
    this.add.text(this.game.width / 2, this.game.height - 75, "sound assets Copyright (c) 2012 - 2013 Drew Showalter", { font: "12px monospace", fill: "#fff", align: "center"}).anchor.setTo(0.5, 0.5);
  },

  update: function () {

    if (this.input.keyboard.isDown(Phaser.Keyboard.Z) || this.input.activePointer.isDown) {
      this.startGame();
    }
    //  Hopefuly i have time to some nice funky main menu effect here
  },

  startGame: function (pointer) {

    //  Ok, the Play Button has been clicked or touched, so stop music (otherwise it'll carry on playing)
    // this.music.stop();

    //  And start the actual game
    this.state.start('Game');

  }

};